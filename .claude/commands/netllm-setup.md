---
description: First-time netllm install from this repo (uv sync, init, discover, verify)
allowed-tools: Read, Shell, Grep
---

Load and follow the skill at `.claude/skills/netllm-setup/SKILL.md` (fallback: `.agents/skills/netllm-setup/SKILL.md`).

Execute the full setup workflow for this machine. Report each step with pass/fail.

- Prefer `./netllm` from the repo root over assuming global PATH.
- Do not commit changes unless the user asks.
- If setup succeeds, offer `/netllm-connect` next.
