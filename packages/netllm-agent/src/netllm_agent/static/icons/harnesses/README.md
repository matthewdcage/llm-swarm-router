# Harness icons

Served at `/ui/icons/harnesses/<harness-id>.svg`, one file per
`netllm_core.known_harnesses.KNOWN_HARNESSES` entry (Sources tab / macOS
Settings "Known harnesses" badges — docs/cli-source-routing-plan.md Phase 4c/4d).

| File | Source | License |
|------|--------|---------|
| `claude-code.svg` | [simple-icons](https://github.com/simple-icons/simple-icons) `claude` slug | CC0-1.0 |
| `codex.svg` | simple-icons `openai` slug (Codex has no distinct mark; reuses the OpenAI wordmark since Codex is an OpenAI product) | CC0-1.0 |
| `gemini-cli.svg` | simple-icons `googlegemini` slug | CC0-1.0 |
| `cursor.svg` | simple-icons `cursor` slug | CC0-1.0 |
| `honcho.svg` | Generated in-repo — no published Honcho mark exists in simple-icons | N/A (original, trivial) |
| `buzz.svg` | Generated in-repo — no published Buzz mark exists in simple-icons | N/A (original, trivial) |

The four simple-icons files are used as pulled from
`https://cdn.jsdelivr.net/npm/simple-icons@latest/icons/<slug>.svg`, unmodified
except for the local filename. simple-icons is CC0-1.0 (public domain) —
see the upstream repo for the current license text. `honcho.svg`/`buzz.svg`
are plain generated monogram placeholders (colored circle + initial), not a
claim of an official mark — swap them out if either project publishes one.
